# /etc/default/nvmf_tgt
# not executable file
# contains environment variables for /etc/systemd/system/nvmf_tgt.service

# run on 4 cores out of 8 on SNIC
CPU_MASK=0xf0
# default config file
NVMF_TGT_CONF=/etc/spdk/nvmf_proxy.conf

# extra args like -r 0.0.0.0 to start RPC listener on
EXTRA_ARGS="-u --wait-for-rpc --mem-size 1200"
